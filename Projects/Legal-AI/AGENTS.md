## REQUIRED

- **NEVER use `rm -rf`** - it's blocked by command-validator hook for safety
- use `trash` instead: `trash folder-name` or `trash file.txt`
    - Workds exactly like `rm -rf` but moves to Trash instead of permanent deletion
